Design implementation plan for adding 4 new providers to /home/user/git/luckydonald/ai-usage, following its existing Provider/LoginMethod/UsageMethod architecture.

Architecture reference (already explored, don't re-explore from scratch — verify by reading, don't rediscover):
- Base classes: src/ai_usage/providers/base.py — `LoginMethod` (key, display_name, credential_kind: "none"|"bearer_token"|"cookie_jar"|"app_token"; discover(), authenticate(options), discover_options(credential)), `UsageMethod` (required_credential_kind; abstract fetch(account, credential) -> ProviderFetchResult), `Provider` (service, key, display_name, configuration_fields, usage_method, login_methods tuple, matching_login_methods(), fetch() delegates to usage_method.fetch(), abstract user_identity()).
- Registry: src/ai_usage/providers/registry.py — built_in_registry() instantiates providers, register by (service,key), supports entry_points(group="ai_usage.providers") for external plugins.
- Existing provider dir shape example (Copilot, simplest style, API-key/bearer-token based, no cookie capture): src/ai_usage/providers/copilot/{provider.py, login/cli/token_reuse.py, usage/web/{billing_api.py,quota_api.py,entitlements.py}}. Note `GenericPrivateWebUsage` in usage/web/entitlements.py:1-75 is explicitly documented as a candidate generic "cookie/header credential in, dotted-path JSON percentage out" helper for reuse — check if it fits any of these new providers or should be promoted to providers/_shared/.
- HTTP client style: web usage-methods each open their own curl_cffi.requests.AsyncSession(impersonate="chrome") (needed for Cloudflare-fronted vendors) or plain httpx.AsyncClient (non-Cloudflare vendors like Copilot). No shared HTTP wrapper class exists yet.
- Credential storage: handled generically via crypto.py CredentialCipher + database.py — new LoginMethod/Provider code does NOT need to touch this directly, cli.py's add-wizard and create_account() wire it automatically once authenticate() returns a credential dict.
- CLI add-wizard: src/ai_usage/cli.py select_provider (~line 420), resolve_login_method (~line 444-455), run_provider_add_wizard (~line 799), provider add command (~line 513-664) — chains service select -> login method select -> authenticate() -> discover_options() -> verify_provider_setup -> create_account. New providers plug into this automatically once registered in registry.py; no CLI changes needed unless a provider needs a genuinely new interaction pattern.

Your task: design the LoginMethod + UsageMethod + Provider implementation for these 4 providers, all using simple static API-key auth (credential_kind="bearer_token" or similar, no OAuth, no cookie capture):

1. **Z.ai** — API key pasted by user. Base https://api.z.ai/, header Authorization: Bearer <apiKey>. Two GET endpoints both required: /api/biz/subscription/list (plan/subscription list, take productName+status), /api/monitor/usage/quota/limit (limits[] array). Response shape: {code,msg,data,success}. limits[] items: {type,unit,number,usage,currentValue,remaining,percentage,nextResetTime}; type=="TOKENS_LIMIT" entries sorted by duration -> shortest="session", 2nd-shortest="weekly"; type=="TIME_LIMIT" -> web-search window. unit encodes granularity (1=days,3=hours,5=minutes,6=weeks; number*unit-ms = period). No-plan detected via empty subscription list + success=false + message containing "coding plan" or Chinese "不存在". 401/403 -> invalid key.

2. **MiniMax** — API key pasted by user, plus a region choice (Global vs CN) stored in provider config (configuration_fields). Region-dependent ordered fallback list of GET endpoints (stop on first 200, else try next, stop entirely on 401/403): Global: https://api.minimax.io/v1/api/openplatform/coding_plan/remains, then .../v1/coding_plan/remains, then https://www.minimax.io/v1/api/openplatform/coding_plan/remains. CN: https://api.minimaxi.com/v1/api/openplatform/coding_plan/remains, then .../v1/coding_plan/remains. Headers: Authorization: Bearer <apiKey>. Response: {base_resp:{status_code,status_msg}, model_remains:[{current_interval_total_count,current_interval_usage_count,current_interval_used_count,current_interval_remaining_count,current_interval_remains_count,start_time,end_time,remains_time,current_subscribe_title,plan_name,plan}]}. status_code==0 means success. Try field-name priority chain for remaining/used counts (current_interval_usage_count -> _remaining_count -> _remains_count); used = total-remaining if no explicit used field. Timestamps may be seconds or millis (heuristic: <10 billion = seconds).

3. **Cline** — API key/secret. Base https://api.cline.bot/api/v1/, GETs /users/me, /users/balance, /users/usages, /users/plan/usage-limits (best-effort combine into ProviderFetchResult).

4. **OpenRouter** — Provisioning key pasted by user. Base https://openrouter.ai/, credits + usage endpoints (find/confirm actual OpenRouter API usage/credits endpoint shape via web knowledge — it's a documented public API, roughly GET /api/v1/credits or /api/v1/auth/key giving {data:{limit,usage,limit_remaining,...}}).

For each provider produce: directory layout (providers/<name>/{__init__.py,provider.py,login/local/api_key.py or similar,usage/web/private_api.py}), the LoginMethod class shape (how the user pastes the key — check how an existing static-secret login method is structured, e.g. if none exists precisely like this, look at TokenReuseLogin in copilot/login/cli/token_reuse.py or CookieCaptureLogin for the interface shape and adapt for a manual-paste flow), the UsageMethod fetch() logic per vendor including response parsing into ProviderFetchResult, the Provider class wiring, and the registry.py registration diff. Note any shared code opportunities across the 4 (e.g. a generic "static API key, bearer auth" LoginMethod that all 4 could share).

Read the actual files mentioned above in the real repo to confirm current signatures before finalizing — do not assume signatures without checking. Return a structured implementation plan: files to create (with responsibilities, not full code), files to modify (registry.py diff), and open questions/risks (e.g. OpenRouter endpoint shape uncertainty, MiniMax plan-name-from-limit heuristic tables).