Design implementation plan for adding 4 new providers to /home/user/git/luckydonald/ai-usage, following its existing Provider/LoginMethod/UsageMethod architecture, PLUS two new shared OAuth login helpers that don't exist yet.

Architecture reference (already explored — verify by reading actual files, don't rediscover from scratch):
- Base classes: src/ai_usage/providers/base.py — `LoginMethod` (key, display_name, credential_kind: "none"|"bearer_token"|"cookie_jar"|"app_token"; discover(), authenticate(options), discover_options(credential)), `UsageMethod` (required_credential_kind; abstract fetch(account, credential) -> ProviderFetchResult), `FallbackUsageMethod` (tries multiple UsageMethods in order), `Provider` (service, key, display_name, configuration_fields, usage_method, login_methods tuple, matching_login_methods(), fetch(), abstract user_identity()).
- Registry: src/ai_usage/providers/registry.py — built_in_registry(), register by (service,key), entry_points(group="ai_usage.providers") support.
- Existing cookie-capture login pattern (COPY THIS for Cursor/Ollama, both are cookie-jar credential_kind): src/ai_usage/webview_login.py — capture_cookies_via_webview(url, title, click_selector=None) -> dict[str,str]. Opens native pywebview window, snapshots cookies on navigation, auto-detects login completion via domain/path change. Used by src/ai_usage/providers/claude/login/web/cookie_capture.py and src/ai_usage/providers/codex/login/web/cookie_capture.py — read both to see the CookieCaptureLogin class shape and adapt.
- HTTP client style: curl_cffi.requests.AsyncSession(impersonate="chrome") for Cloudflare-fronted vendors (see src/ai_usage/providers/claude/usage/web/private_api.py — has local get_json()/describe_fetch_failure() helpers ~lines 25-58 for labeled HTTP-error handling with reauth_hint). Plain httpx.AsyncClient for non-Cloudflare vendors.
- IMPORTANT project memory: "Native GUI needs a real run" — pywebview/native-GUI code needs genuine-tty smoke test, mocks can't catch thread/toolkit failures. This env has NO tty (per project memory) so cookie-capture-based login code cannot be manually verified interactively here — plan should note this as a known verification gap, testable only by the user locally.
- No PKCE or device-flow OAuth helper exists anywhere in ai-usage today. You must design these as NEW shared modules.

Your task: design 4 providers using patterns as follows:

1. **Cursor** (credential_kind="cookie_jar", reuse capture_cookies_via_webview against https://cursor.com login, or if cursor.com doesn't support webview cookie flow, evaluate whether a manual paste-based login (like the ai-usage's existing SettingsFileLogin style, but for a manually-pasted cookie string) fits better since Cursor's cookie is a compound `userId::accessToken` JWT value not a full site session in some flows). Usage: try web path first (GET https://cursor.com/api/usage-summary, https://cursor.com/api/auth/me best-effort, https://cursor.com/api/usage?user=<id>), fall back to gRPC-JSON path (POST https://api2.cursor.sh/aiserver.v1.DashboardService/GetCurrentPeriodUsage body "{}", GetPlanInfo best-effort, GET /auth/full_stripe_profile best-effort) using Authorization: Bearer <jwt-extracted-from-cookie>. Design a CursorSessionTokenParser equivalent: URL-decode cookie, split on "::", take JWT (or whole value if startswith "eyJ"). Money fields in cents, divide by 100. Use FallbackUsageMethod (web path then gRPC path) matching the existing pattern in base.py.

2. **Ollama cloud** (credential_kind="cookie_jar" via capture_cookies_via_webview against ollama.com, capturing `__Secure-session` cookie; optional secondary `cf_clearance` cookie). Usage: GET https://ollama.com/settings — response is HTML, NOT JSON — must be scraped (find/confirm a Python HTML parsing lib already in ai-usage's dependencies, e.g. beautifulsoup4 or lxml or selectolax; if none present, recommend one and note it as a new dependency). Extract plan from nested class="capitalize" span inside an <h2> "Cloud Usage" section; extract usage percentage from progress-bar style attribute matching regex width:\s*([0-9.]+)% by walking up from a <span class="text-sm"> labeled "Session usage"/"Weekly usage"; extract reset time from a [data-time] attribute (ISO instant). This is the ONLY HTML-scraping usage method in the codebase — flag as an architectural first, note UsageMethod.fetch() can return raw HTML in ProviderFetchResult.raw_payload same as JSON providers do currently.

3. **SuperGrok (xAI)** — needs a NEW shared PKCE-with-local-loopback-listener OAuth helper (does not exist in ai-usage; existing login is cookie-jar or local-token-reuse only, no OAuth-code-exchange anywhere). Design src/ai_usage/providers/_shared/oauth_pkce.py (or similar shared location — check if providers/_shared/ already exists, else propose creating it) providing a generic PKCE helper: generate code_verifier/code_challenge, open system browser to an authorize URL, run a short-lived local HTTP server on a fixed port to catch the redirect callback with code+state, exchange code for tokens via POST to a token endpoint. xAI specifics: authorization_endpoint https://auth.x.ai/oauth2/authorize, client_id b1a00492-073a-47ea-816f-4c329264a828, redirect_uri http://127.0.0.1:56121/callback, scopes "openid profile email offline_access grok-cli:access api:access", token endpoint https://auth.x.ai/oauth2/token. Usage: GET https://cli-chat-proxy.grok.com/v1/billing?format=credits (Authorization: Bearer, X-XAI-Token-Auth: xai-grok-cli), optional GET .../settings for plan display name. Parse manually from raw JsonObject (root may be {config:{...}} or {billing:{config:{...}}}).

4. **Kimi (Moonshot)** — needs a NEW shared OAuth device-flow helper (does not exist in ai-usage). Design src/ai_usage/providers/_shared/oauth_device_flow.py: POST to a device_authorization endpoint (form client_id + scope), get back device_code/user_code/verification_uri/expires_in/interval, print/display verification_uri+user_code to user (adapt to ai-usage's CLI/TUI output style — check cli.py for how it currently surfaces such prompts, e.g. rich console or plain print), poll a token endpoint until success or timeout, handle authorization_pending/slow_down/expired_token. Kimi specifics: device_authorization endpoint https://auth.kimi.com/api/oauth/device_authorization, client_id 17e5f671-d194-4dfb-9706-5516cb48c098, token endpoint https://auth.kimi.com/api/oauth/token. Usage: GET https://api.kimi.com/coding/v1/usages, Authorization: Bearer <accessToken>. Response: {usage, limits:[{window:{duration,timeUnit}, detail:{limit,remaining,resetTime}}], user:{membership:{level}}}. limit/remaining are STRINGS. Session window = limit where window.duration==300 && timeUnit=="TIME_UNIT_MINUTE", fallback limits[0]. used = limit-remaining clamped >=0.

For each provider produce: directory layout, LoginMethod class shape, UsageMethod fetch()+parsing logic, Provider wiring, registry.py diff. For the 2 new shared OAuth helpers, produce their public API shape (function signatures / class shape) and note which existing modules (if any) they should live near, and whether SuperGrok/Kimi's provider-specific login classes are thin wrappers around the shared helper (preferred) vs bespoke.

Read the actual files mentioned above in the real repo to confirm current signatures/behavior before finalizing — do not assume without checking, especially base.py's exact LoginMethod/UsageMethod/FallbackUsageMethod signatures and ProviderFetchResult's fields. Return a structured implementation plan: files to create, files to modify, shared-helper design, and open questions/risks (e.g. no-tty verification gap for webview cookie capture, HTML-scraping fragility for Ollama, whether local loopback port 56121 could conflict, whether ai-usage's CLI can currently even prompt/display a device-flow user_code mid-wizard).