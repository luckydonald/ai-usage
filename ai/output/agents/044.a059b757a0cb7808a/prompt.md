Repo: /home/user/git/luckydonald/ai-usage. You're restructuring src/ai_usage/providers/copilot.py (276 lines) and src/ai_usage/providers/web.py (72 lines) into a login/usage split. Read the full plan first: /home/user/.claude/plans/merry-purring-wozniak.md (context, design, directory layout, "what changes at edges" — ignore the cli.py wizard rewrite section, someone else handles that).

Base abstractions already exist in src/ai_usage/providers/base.py (already edited — read it first): `LoginMethod(ABC)` (key, display_name, credential_kind: "none"|"bearer_token"|"cookie_jar"|"app_token"; async discover()->list[DiscoveredAccount], async authenticate(options)->dict|None, async discover_options(credential)->dict), `UsageMethod(ABC)` (required_credential_kind; abstract async fetch(account, credential)->ProviderFetchResult), `FallbackUsageMethod(UsageMethod)`, and `Provider(ABC)` which now takes `usage_method: UsageMethod` + `login_methods: tuple[LoginMethod,...]` and delegates discover/authenticate/discover_options/fetch. `user_identity` is implemented directly on the concrete Provider subclass (override it, same as before).

Read src/ai_usage/providers/copilot.py and src/ai_usage/providers/web.py fully. copilot.py has: `next_billing_reset` helper, `CopilotBillingProvider(Provider)` (service=copilot key=github-api: fetch via httpx GitHub billing API using a pre-supplied credential["token"], no authenticate/discover defined — credential supplied externally), `copilot_cli_credentials(config_dir)` free function (reads local ~/.copilot/config.json or env vars COPILOT_GITHUB_TOKEN/GH_TOKEN/GITHUB_TOKEN to reuse an existing OAuth token — currently called from inside CopilotStatusProvider.fetch), `copilot_quota_metric_key`/`parse_copilot_quota_payload` parsing helpers, `CopilotStatusProvider(Provider)` (service=copilot key=statusline: discover() probes ~/.copilot/config.json; fetch() calls copilot_cli_credentials() THEN does an httpx call to /copilot_internal/user — the copilot_cli_credentials() call is a login-time concern currently embedded inline in fetch and must move OUT into the LoginMethod, so fetch() only receives an already-resolved credential). web.py has `PrivateWebProvider(Provider)` (experimental=True, generic cookie/header-based httpx fetch with dotted-path field extraction, no authenticate/discover — credential assumed pre-supplied; user_identity raises ProviderLoginError unconditionally) and `CopilotEntitlementsProvider(PrivateWebProvider)` (service=copilot key=entitlements, just sets class attrs).

Target layout (create; add __init__.py for package dirs):
- providers/copilot/__init__.py
- providers/copilot/login/__init__.py
- providers/copilot/login/cli/__init__.py, providers/copilot/login/cli/token_reuse.py — `TokenReuseLogin(LoginMethod)` wrapping `copilot_cli_credentials` (moved here, called from this LoginMethod's discover() or authenticate() — read the original call site in CopilotStatusProvider.fetch carefully to decide whether it's a discover-only local reuse or should also be reachable via authenticate; likely `discover()` since it's reusing something already configured locally, similar to a file probe). credential_kind="bearer_token".
- providers/copilot/login/local/__init__.py, providers/copilot/login/local/config_file.py — `ConfigFileLogin(LoginMethod)` wrapping the ~/.copilot/config.json discover probe from CopilotStatusProvider.discover. Decide if this is the same as TokenReuseLogin (copilot_cli_credentials already reads that same config.json) or genuinely separate — read the code carefully; if discover() and copilot_cli_credentials() overlap significantly, it's fine to have ONE LoginMethod class covering both (your call, document reasoning).
- providers/copilot/usage/__init__.py
- providers/copilot/usage/web/__init__.py, providers/copilot/usage/web/billing_api.py — `BillingApiUsage(UsageMethod)` wrapping CopilotBillingProvider.fetch (httpx GitHub billing API) + next_billing_reset helper. required_credential_kind — CopilotBillingProvider takes a pre-supplied external token with no LoginMethod of its own in-code; pick "bearer_token" and give CopilotBillingProvider an empty login_methods tuple (credential supplied by CLI flag/manual entry, matching original behavior of no authenticate/discover).
- providers/copilot/usage/web/quota_api.py — `QuotaApiUsage(UsageMethod)` wrapping CopilotStatusProvider.fetch's httpx /copilot_internal/user call (WITHOUT the inline copilot_cli_credentials call, which moved to the login method) + copilot_quota_metric_key/parse_copilot_quota_payload. required_credential_kind="bearer_token".
- providers/copilot/usage/web/entitlements.py — move `PrivateWebProvider` here as a *generic reusable base* — but note PrivateWebProvider currently IS a Provider subclass, not a UsageMethod. Since PrivateWebProvider has no login logic (credential pre-supplied, user_identity unconditionally raises) it fits fine as a generic `GenericPrivateWebUsage(UsageMethod)` (the httpx+dotted-path-extraction fetch logic) used by a `CopilotEntitlementsProvider(Provider)` with empty login_methods — required_credential_kind: pick "bearer_token" or a more generic kind, your call, document it. Since this class was written to be generic/reusable (experimental=True, base for future non-copilot providers too), consider whether the UsageMethod itself should stay in providers/base.py instead of copilot/ specifically (re-read the plan's note about this exact ambiguity) — your call, pick one and document why.
- providers/copilot/provider.py — assemble `CopilotBillingProvider`, `CopilotStatusProvider` (TokenReuseLogin/ConfigFileLogin + QuotaApiUsage), `CopilotEntitlementsProvider`. Preserve exact service/key/display_name/experimental/configuration_fields/login_url/login_hint/icon/user_identity per original classes.

Delete src/ai_usage/providers/copilot.py once fully migrated. For web.py: delete it if PrivateWebProvider fully relocates into copilot/ or base.py; if you decide it should stay as a cross-provider generic export point, keep a slim web.py that just re-exports from its new home (prefer deleting it entirely if nothing outside copilot currently uses it — grep first: `grep -rn "providers.web\|providers import web\|from ai_usage.providers.web" src/ tests/`).

Update tests/test_providers.py's import lines for copilot symbols ONLY (search `from ai_usage.providers.copilot import` in tests/test_providers.py) — surgical edit to just those lines. Other agents are editing the same test file concurrently for claude/codex imports — do NOT touch any other lines in that file to avoid edit conflicts.

At the end, run: cd /home/user/git/luckydonald/ai-usage && python -c "from ai_usage.providers.copilot.provider import *" to confirm it imports cleanly (fix errors). Do not run the full test suite yet (other agents' files aren't done).

Report back: exact new file paths, exact class/function names exported from provider.py, and any deviations from plan with rationale (especially the ConfigFileLogin/TokenReuseLogin overlap decision and the PrivateWebProvider/web.py placement decision).