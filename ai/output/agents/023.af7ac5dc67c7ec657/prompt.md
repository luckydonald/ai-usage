Repo: /home/user/git/luckydonald/ai-usage. Vue 3 + TS frontend in frontend/src/. I'm planning several fixes to the usage-graph tooltip and the "info panels" below the graph.

Investigate and report, with file:line citations:

1. **Notes/promo rendering in the tooltip.** Read frontend/src/chart.ts's `noteTooltipHtml`, `activeNotesAt`, and where they're called from `axisTooltipHtml` (the notes are appended at the very end, after all series rows). Confirm: does the note `text` field ever contain markdown syntax like `**bold**` or `[link](url)`? Check `NoteRange`'s `text` field usage/origin — trace back through frontend/src/types.ts, frontend/src/api.ts's `fetchNotes()`, and (backend side) src/ai_usage/notes.py / src/ai_usage/providers/claude.py's `extract_claude_notes`/`extract_claude_web_notes` / src/ai_usage/providers/codex.py's `extract_codex_notes` — do any of these produce raw markdown text (e.g. the GrowthBook promo banner text captured earlier this session, which had literal `**bold**` markdown in it)? Is there currently ANY markdown-rendering library in frontend/package.json (search for `marked`, `markdown-it`, `dompurify`, or similar)? Is there any HTML-sanitization step anywhere before `v-html` is used (grep `v-html` across frontend/src)?

2. **Tooltip overflow/bounds behavior.** In frontend/src/chart.ts's `chartOption()`, find the `tooltip: { confine: true, ... }` setting and the tooltip's `backgroundColor`/styling. Does `confine: true` cause ECharts to reposition/shrink the tooltip to stay within the chart's bounding box? Is there any max-width/max-height CSS constraint on the tooltip content itself (search for any tooltip-related CSS in frontend/src/components/UsageChart.vue or frontend/src/styles/)?

3. **Page-level active-notes banner.** Read frontend/src/App.vue in full — specifically the `notes` ref, `loadNotes()`, and where `notes` is currently only passed down to `<UsageChart :notes="notes" />`. Is there any existing page-level banner-style display for anything already (e.g. the "This dashboard is exposed without authentication" banner — check its markup/CSS class `banner`/`banner-error` in frontend/src/App.vue and frontend/src/styles/main.scss) that a new "active promos" banner could reuse the visual style of?

4. **ServicePanels.vue time display.** Read frontend/src/components/ServicePanels.vue in full. Find every place it renders a date/time (window start/end, "resets at", etc. — likely reusing `windowTooltipHtml`/`pointTooltipHtml` from chart.ts per earlier session context saying these are "reused directly (not via echarts) in ServicePanels.vue via v-html"). Confirm exactly which absolute-time strings currently get shown (e.g. `new Date(...).toLocaleString()`) and whether frontend/src/time.ts already has any relative-time formatting helper (grep for "ago", "in ", "relative" in frontend/src/time.ts) — if not, note that one would need to be added, and check what formatDuration() there already does since it might be reusable/adjacent.

5. Check frontend/src/chart.test.ts and any ServicePanels test file for existing coverage of the above, so a future plan knows what tests already pin this behavior down.

Report concisely but precisely — this feeds directly into an implementation plan for: rendering notes as sanitized markdown positioned at the top of the tooltip without causing overflow-clipping, a new page-level "active promos" banner, and relative-time-with-absolute-tooltip display in ServicePanels.vue.