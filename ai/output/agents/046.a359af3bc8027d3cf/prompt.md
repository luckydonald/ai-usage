Repo: /home/user/git/luckydonald/ai-usage. You're restructuring the Claude provider files under src/ai_usage/providers/ into a login/usage split. Read the full plan first: /home/user/.claude/plans/merry-purring-wozniak.md (context, design, directory layout, "what changes at edges" sections — ignore the cli.py wizard section, someone else is doing that).

The base abstractions already exist in src/ai_usage/providers/base.py (already edited — read it first): `LoginMethod(ABC)` (key, display_name, credential_kind: "none"|"bearer_token"|"cookie_jar"|"app_token"; async discover()->list[DiscoveredAccount], async authenticate(options)->dict|None, async discover_options(credential)->dict), `UsageMethod(ABC)` (required_credential_kind; abstract async fetch(account, credential)->ProviderFetchResult), `FallbackUsageMethod(UsageMethod)` (wraps ordered tuple of UsageMethod, tries each catching ProviderError), and `Provider(ABC)` now takes `usage_method: UsageMethod` and `login_methods: tuple[LoginMethod,...]` and delegates discover/authenticate/discover_options/fetch/user_identity accordingly (user_identity stays abstract-ish on Provider subclass, override it same as before).

Current files to consume and then DELETE once split: src/ai_usage/providers/claude_cli.py, claude_direct.py, claude_interactive.py, and src/ai_usage/providers/claude/api.py, __init__.py, relay.py, status.py. Read all of these fully first.

Target layout (create these, package dirs need __init__.py where used as packages):
- providers/claude/_shared.py — claude_environment, claude_profile_path, canonical_claude_profile_dir, claude_default_profile (moved verbatim from claude_cli.py), plus claude_setup_required_message, write_claude_error_log, claude_cli_failure_message (also from claude_cli.py) since both login and usage code need them.
- providers/claude/login/__init__.py, providers/claude/login/local/__init__.py, providers/claude/login/local/settings_file.py — a `SettingsFileLogin(LoginMethod)` class wrapping the local `~/.claude/settings.json` probe logic that was `ClaudeStatusProvider.discover` in status.py. credential_kind = "none" (no credential needed, just confirms local CLI is set up).
- providers/claude/login/web/__init__.py, providers/claude/login/web/cookie_capture.py — a `CookieCaptureLogin(LoginMethod)` wrapping `capture_cookies_via_webview` (authenticate) and the org_id HTTP discovery (discover_options) that were on `ClaudeWebUsageProvider` in api.py. credential_kind = "cookie_jar".
- providers/claude/usage/__init__.py
- providers/claude/usage/web/__init__.py, providers/claude/usage/web/private_api.py — the HTTP fetch logic + payload models + parsing (parse_claude_web_usage, extract_claude_web_notes, etc.) from api.py, as a `PrivateApiUsage(UsageMethod)`. required_credential_kind = "cookie_jar".
- providers/claude/usage/local/__init__.py, providers/claude/usage/local/relay_file.py — the relay-JSON-file-read portion of `ClaudeStatusProvider.fetch` (status.py), as a `RelayFileUsage(UsageMethod)`. required_credential_kind = "none".
- providers/claude/usage/cli/__init__.py, providers/claude/usage/cli/direct.py — run_claude_usage_direct (moved from claude_direct.py) wrapped as `DirectCliUsage(UsageMethod)`.
- providers/claude/usage/cli/interactive.py — run_claude_usage (moved from claude_interactive.py, pexpect PTY) wrapped as `InteractiveCliUsage(UsageMethod)`.
- providers/claude/relay.py — keep install_status_relay/remove_status_relay as-is (free functions, not a LoginMethod/UsageMethod), just update its imports if it referenced anything that moved (check first — from the plan, relay.py itself is standalone content, but double check for any import from claude_cli.py etc and fix path).
- providers/claude/provider.py — assemble three `Provider` instances: `claude_web_usage_provider` (or class `ClaudeWebUsageProvider(Provider)`, service="claude", key="web") composed of CookieCaptureLogin + PrivateApiUsage; `claude_status_provider` (service="claude", key="statusline") composed of SettingsFileLogin + FallbackUsageMethod((RelayFileUsage, DirectCliUsage, InteractiveCliUsage)) matching the original fallback order in ClaudeStatusProvider.fetch (check status.py's actual fetch logic/order and relay-freshness-check condition carefully — preserve exact behavior, including the "relay file must be fresh" logic before falling through to CLI); `claude_cli_usage_provider` (service="claude", key="cli-usage") — same as statusline but CLI-only fallback chain (no relay), matching original `ClaudeUsageProvider.fetch` behavior which stripped the relay_file option. Keep `user_identity` implementations on each Provider subclass matching original per-provider logic (was on ClaudeStatusProvider/ClaudeWebUsageProvider in the old files — check status.py:179 and api.py:188). Also keep `run_claude_auth_status` (status.py:122) somewhere sensible — it's used inside the CLI usage fetch path for identity resolution; probably belongs in providers/claude/usage/cli/direct.py or a shared helper, check exactly where/how it's called in current status.py fetch and preserve that call site's behavior.
- providers/claude/__init__.py — re-export whatever cli.py / registry.py / tests actually need (check with grep -rn 'from ai_usage.providers.claude\|from ai_usage.providers import' /home/user/git/luckydonald/ai-usage/src /home/user/git/luckydonald/ai-usage/tests before deciding names — but per the plan we are NOT required to preserve old class names, feel free to rename to clearer new names as long as you also update every import site across src/ and tests/ that references the old names). Do NOT touch src/ai_usage/cli.py, src/ai_usage/collector.py, src/ai_usage/provider_discovery.py, src/ai_usage/providers/registry.py — someone else is updating those; just leave a clean new `providers/claude/provider.py` with clearly-named exports (e.g. `ClaudeWebUsageProvider`, `ClaudeStatusProvider`, `ClaudeUsageProvider` are fine names to keep, or rename — your call, just be consistent) that those files can import once their own step runs.

Delete the old files once fully migrated: claude_cli.py, claude_direct.py, claude_interactive.py, and the old contents of claude/api.py, claude/status.py (replace with nothing — actually delete those files entirely, don't leave empty stubs).

Update tests/test_providers.py's import lines for claude symbols (search `from ai_usage.providers.claude import` in tests/test_providers.py) to point at the new module/class names — but ONLY the claude-related import lines, don't touch codex/copilot import lines (other agents handle those in parallel, editing the same file — so make MINIMAL, surgical edits to just the claude import lines in tests/test_providers.py to avoid conflicting edits).

At the end, run: cd /home/user/git/luckydonald/ai-usage && python -c "from ai_usage.providers.claude.provider import *" to sanity check the new module imports cleanly (fix any errors). Do NOT run the full test suite (other agents' files aren't done yet, it will fail for unrelated reasons) — just confirm this module imports without error and grep for any remaining references to the deleted files (claude_cli, claude_direct, claude_interactive) that you haven't fixed: grep -rn "claude_cli\|claude_direct\|claude_interactive" src/ tests/ (excluding this new claude/_shared.py etc. which is expected).

Report back a summary of what you did, exact new file paths, exact class/function names exported, and any deviations from the plan you had to make and why.