Repo: /home/user/git/luckydonald/ai-usage. Python FastAPI backend in src/ai_usage/ (main app assembly in src/ai_usage/api.py). I want to add a new endpoint that serves brand SVG icons from the `fontawesomefree` pip package (already installable via pip/uv — check if it's already a dependency in pyproject.toml, and if not, note that).

Investigate and report:
1. Is `fontawesomefree` already installed in this project's venv (check `.venv` or run `python -c "import fontawesomefree; print(fontawesomefree.__file__)"` via the project's `uv run` if available, or `pip show fontawesomefree` / find it in site-packages)? If it IS installed, find the actual on-disk directory layout of the package — specifically where the "brands" SVG icon files live (Font Awesome free icons are normally organized as `svgs/brands/*.svg`, `svgs/solid/*.svg`, etc. inside the packaged static assets) and confirm exact relative path structure, confirm files like `claude.svg`, `github.svg` etc. exist, and get the package's installed version string (for use in a version-pinned URL path like `/img/icons/fontawesomefree/v{version}/brands/{icon}.svg`).
2. If `fontawesomefree` is NOT installed, just report that clearly (don't try to install it) and note what the typical Font Awesome Free pip package's directory layout looks like based on any info you can gather (check if it's referenced anywhere in the repo already, e.g. in requirements/lockfiles/docs).
3. Read src/ai_usage/api.py in full — especially the FastAPI app assembly/startup code, the existing `app.mount("/assets", StaticFiles(directory=assets), name="assets")` call (found in prior work this session) and its surrounding context — what triggers that mount (is `assets` conditional on the frontend dist existing?), and what other routes/routers exist. I need to know the idiomatic way to add a new route in this specific FastAPI app: is it decorated directly on a shared `app`/`router` object, or are there sub-routers per feature (look for `APIRouter` usage, `include_router` calls)?
4. Check whether this project already sets any HTTP caching headers anywhere (search for `Cache-Control`, `max-age`, `immutable`, `RedirectResponse`, `FileResponse` across src/ai_usage/*.py) — I need a pattern to follow for (a) a long-lived "cache forever" response for a version-pinned icon file, and (b) a short-lived (~1 day) cached redirect response for a "latest" alias route.
5. Check pyproject.toml's full dependency list once more to see if there's already an SVG-related or static-asset-related dependency I should be aware of, and confirm the Python version constraint (for typing/pathlib API choices).

Report file:line citations. This feeds directly into a plan for a new icon-serving endpoint, so precision matters more than speed.