In this repo (/home/user/git/luckydonald/ai-usage), find the CLI code for `provider add` command, specifically its "advanced" section that lists already-discovered providers/accounts. The bug report: "When running `provider add`, in the advanced section, the already discovered ones are repeated." Find the file(s) and function(s) responsible for building that advanced-section list, and identify why already-discovered providers/accounts might be listed again (duplication bug). Quick search, report file:line and relevant code snippets, under 300 words.