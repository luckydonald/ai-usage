Repo: /home/user/git/luckydonald/ai-usage (Python FastAPI backend in src/ai_usage/, Vue 3 + TS frontend in frontend/src/). The dashboard's toolbar shows filter "chip" badges for services/providers/accounts/metrics (e.g. "claude", "codex", "statusline", "web", "app-server" as seen in frontend/src/App.vue).

I need to answer precisely: **how does the frontend currently figure out which filter chips to display?** Specifically:
1. Read frontend/src/App.vue in full — find the `services`, `providers`, `accounts`, `metricOptions` computed properties and the `<div class="chip-group">` template blocks that render them. Are the displayed chip labels (e.g. "claude", "codex", "statusline") literally the raw `service`/`provider` string values coming from the backend catalog API response, or is there any hardcoded mapping/display-name logic in the Vue code itself?
2. Read frontend/src/api.ts and frontend/src/types.ts — find the `fetchCatalog()` function and the `Catalog`/`CatalogMetric`/`Account` type shapes. What fields does the `/api/v1/catalog` response actually contain for each metric/account (service, provider, account_id, etc.)?
3. Read src/ai_usage/api.py's catalog endpoint (search for `catalog` or `/api/v1/catalog`) — where does it source the `service`/`provider` strings from? Trace back to src/ai_usage/providers/base.py's `Provider` class (or wherever `service`/`key`/`display_name` class attributes are defined) — is there already a `display_name` class attribute per provider (I recall `ClaudeWebUsageProvider.display_name = "Claude private web API"` from prior work this session)? Does the catalog response include `display_name`, or only the raw `service`/`provider` key strings?
4. List every distinct `service` value across all provider classes in src/ai_usage/providers/*.py (grep for `service = "..."`) — I need the exact set of brand identifiers in use (e.g. "claude", "codex", "copilot", others?) to plan which icon files I'll need.
5. Also check src/ai_usage/providers/base.py's `Provider` base class for any existing icon/branding-related class attribute (e.g. `color`, `icon`, `brand`) — earlier work this session added a "generated_color per brand hue" function in frontend/src/chart.ts (`generated_color`), so check whether service->color mapping is done frontend-side (hardcoded) or backend-side, as a model for how service->icon mapping should likely be done too.

Report precisely: is chip generation "dynamic from the catalog API response" or "some hardcoded Vue mapping"? Give file:line citations for everything. This determines whether new brand icons should be wired in as a backend Provider class constant (propagated through the catalog API) or as a frontend-only lookup table.