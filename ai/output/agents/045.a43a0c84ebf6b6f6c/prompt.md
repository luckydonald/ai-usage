Repo: /home/user/git/luckydonald/ai-usage. You're restructuring src/ai_usage/providers/codex.py (522 lines) into a login/usage split. Read the full plan first: /home/user/.claude/plans/merry-purring-wozniak.md (context, design, directory layout, "what changes at edges" — ignore the cli.py wizard rewrite section, someone else handles that).

Base abstractions already exist in src/ai_usage/providers/base.py (already edited — read it first): `LoginMethod(ABC)` (key, display_name, credential_kind: "none"|"bearer_token"|"cookie_jar"|"app_token"; async discover()->list[DiscoveredAccount], async authenticate(options)->dict|None, async discover_options(credential)->dict), `UsageMethod(ABC)` (required_credential_kind; abstract async fetch(account, credential)->ProviderFetchResult), `FallbackUsageMethod(UsageMethod)`, and `Provider(ABC)` which now takes `usage_method: UsageMethod` + `login_methods: tuple[LoginMethod,...]` and delegates discover/authenticate/discover_options/fetch accordingly. `user_identity` is still implemented directly on the concrete Provider subclass (override it, same as before).

Read src/ai_usage/providers/codex.py fully. It has: parsing helpers (codex_model, extract_codex_notes, window_key, window_name, parse_rate_limits, codex_app_server_email, payload models CodexRateWindowPayload/CodexRateLimitPayload/CodexWhamUsagePayload, parse_codex_web_usage), `CodexWebUsageProvider(Provider)` (service=codex key=web: authenticate via capture_cookies_via_webview at line ~224, fetch via curl_cffi HTTP at ~234), `AppServerClient` (JSON-RPC over stdio subprocess helper, not itself a Provider), `CodexAppServerProvider(Provider)` (service=codex key=app-server: discover() probes ~/.codex/auth.json at ~391, fetch() at ~399 runs codex app-server subprocess via AppServerClient AND inline writes a temp auth.json file from credential["auth_json"] at ~407-413 — this inline credential-write must move OUT of fetch and into the LoginMethod's job, i.e. the LoginMethod should hand fetch() an already-resolved dict/temp-file-ready credential, not have fetch() do file writes for login purposes), `parse_codex_status`, `CodexStatusProvider(Provider)` (service=codex key=cli-status: user_identity raises ProviderLoginError — no identity support; fetch via `run_codex_status` pexpect PTY sending /status).

Target layout (create; add __init__.py for package dirs):
- providers/codex/__init__.py
- providers/codex/login/__init__.py
- providers/codex/login/local/__init__.py, providers/codex/login/local/auth_json.py — `AuthJsonLogin(LoginMethod)` wrapping the ~/.codex/auth.json discover probe. credential_kind="app_token" (or your judgement on the right label — the credential here is the raw contents of auth.json, used later to write a temp file for the app-server subprocess). Since the original fetch() wrote a temp auth.json file from credential["auth_json"], and that's specifically a login-time resolution step, consider whether that temp-file materialization belongs in this LoginMethod's discover()/authenticate() (producing a credential dict that already contains a ready-to-use temp path) or is deferred until fetch-time because app-server subprocess lifetime ties the temp file's lifetime to the fetch call — read the original code carefully (codex.py fetch, ~lines 399-440ish) to decide correctly; document your decision in your final report if you deviate from "move it entirely into login".
- providers/codex/login/web/__init__.py, providers/codex/login/web/cookie_capture.py — `CookieCaptureLogin(LoginMethod)` wrapping capture_cookies_via_webview (authenticate), credential_kind="cookie_jar".
- providers/codex/usage/__init__.py
- providers/codex/usage/web/__init__.py, providers/codex/usage/web/private_api.py — HTTP fetch (curl_cffi) + all the payload models + parsing helpers (parse_codex_web_usage, codex_model, extract_codex_notes, window_key, window_name, parse_rate_limits, CodexRateWindowPayload/CodexRateLimitPayload/CodexWhamUsagePayload), as `PrivateApiUsage(UsageMethod)`, required_credential_kind="cookie_jar".
- providers/codex/usage/cli/__init__.py, providers/codex/usage/cli/app_server.py — `AppServerClient` (unchanged) + `AppServerUsage(UsageMethod)` wrapping the app-server fetch logic, required_credential_kind matching whatever you picked for AuthJsonLogin, codex_app_server_email helper too.
- providers/codex/usage/cli/status_ptv.py — `run_codex_status` (pexpect PTY) + `parse_codex_status` + `StatusPtyUsage(UsageMethod)` (required_credential_kind="none").
- providers/codex/provider.py — assemble `CodexWebUsageProvider` (CookieCaptureLogin + PrivateApiUsage), `CodexAppServerProvider` (AuthJsonLogin + AppServerUsage), `CodexStatusProvider` (no login_methods needed, or an empty tuple + StatusPtyUsage). Preserve each Provider's exact `service`/`key`/`display_name`/`experimental`/`configuration_fields`/`login_url`/`login_hint`/`icon`/`user_identity` behavior from the original classes — check codex.py carefully for these class attrs and copy them onto the reassembled Provider subclasses (they may be set as class attributes directly on the Provider subclass, that's fine and expected — Provider subclasses still set service/key/display_name themselves per base.py).

Delete src/ai_usage/providers/codex.py once fully migrated (don't leave an empty stub).

Update tests/test_providers.py's import lines for codex symbols ONLY (search `from ai_usage.providers.codex import` in tests/test_providers.py) — surgical edit to just those lines. Other agents are editing the same test file concurrently for claude/copilot imports — do NOT touch any other lines in that file to avoid edit conflicts.

At the end, run: cd /home/user/git/luckydonald/ai-usage && python -c "from ai_usage.providers.codex.provider import *" to confirm it imports cleanly (fix errors). Do not run the full test suite (other agents' files aren't done). Also run: grep -rn "providers\.codex\b\|providers import codex\|from ai_usage.providers import codex" src/ tests/ and fix any remaining references to the old flat codex.py module path you find (excluding your own new codex/ package, which is expected to match `providers.codex`).

Report back: exact new file paths, exact class/function names exported from provider.py, and any deviations from plan with rationale (especially your AuthJsonLogin/temp-file decision).