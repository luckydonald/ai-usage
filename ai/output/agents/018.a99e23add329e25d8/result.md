Repository root at `/home/user/git/luckydonald/ai-usage` contains:

- `.agents/`, `AGENTS.md` (symlink to CLAUDE.md), `ai/`, `alembic/`, `alembic.ini`
- `.claude/`, `CLAUDE.md`
- `.codex/`, `dist/`
- `.editorconfig`, `.env.example`
- `frontend/` (has uncommitted changes: `App.vue`, `chart.ts`, `UsageChart.vue`)
- `.git/`, `.gitattributes`, `.github/`, `.gitignore`
- `.idea/`, `.mcp.json`, `.pre-commit-config.yaml`, `.pre-commit-hooks.yaml`
- `pyproject.toml`, `.pytest_cache/`, `README.md`, `.ruff_cache/`, `.run/`
- `scripts/`, `src/`, `tests/`, `uv.lock`, `.venv/`

Mix of Python (pyproject.toml, alembic, src, uv.lock) and a Vue/TS frontend project.